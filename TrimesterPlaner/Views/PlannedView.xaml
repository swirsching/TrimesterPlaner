<UserControl
    x:Class="TrimesterPlaner.Views.PlannedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TrimesterPlaner.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:TrimesterPlaner.ViewModels"
    d:DataContext="{d:DesignInstance {x:Type vm:PlannedViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <ProgressBar
            Width="30"
            Height="20"
            Background="#9BD8F5"
            BorderBrush="#EF9C96"
            Foreground="#039EE6"
            Maximum="1"
            Minimum="0"
            Value="{Binding PlannedPercentage, Mode=OneWay}" />
        <ProgressBar
            Width="50"
            Height="20"
            Background="Transparent"
            BorderThickness="0"
            Foreground="#EF9C96"
            Maximum="3"
            Minimum="1"
            Value="{Binding PlannedPercentage, Mode=OneWay}" />
    </StackPanel>
</UserControl>